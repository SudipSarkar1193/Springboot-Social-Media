# --- DATABASE CONFIGURATION FOR A PERSISTENT H2 TEST DATABASE ---
# --- DATABASE CONFIGURATION FOR IN-MEMORY H2 TEST DATABASE ---
spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# --- JPA/HIBERNATE CONFIGURATION ---
# Use 'create-drop' to automatically build and destroy the database for tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true

#-----------------------------------------

## --- DATABASE CONFIGURATION FOR A PERSISTENT H2 TEST DATABASE ---
#spring.datasource.url=${DATA_SRC_URL}
#spring.datasource.username=${DB_USERNAME}
#spring.datasource.password=${DB_PASSWORD}
#spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
#spring.jpa.database-platform=org.hibernate.dialect.SQLServerDialect
#
## --- JPA/HIBERNATE CONFIGURATION ---
#spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true